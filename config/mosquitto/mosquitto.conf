# Mosquitto MQTT broker configuration
# Deployed on app-net only — no external exposure

listener 1883
allow_anonymous true

# Persistence — retain messages survive broker restart
persistence true
persistence_location /mosquitto/data/

# Logging
log_type all
log_dest stdout
